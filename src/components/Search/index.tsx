import { Container } from '../../styles'

import { Form, Div, SearchTitle } from './styles'

const Search = () => (
  <Form>
    <Div className={Container}>
      <SearchTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </SearchTitle>
    </Div>
  </Form>
)

export default Search
